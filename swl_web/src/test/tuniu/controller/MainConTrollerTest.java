package tuniu.controller;

import org.junit.Test;
import org.springframework.http.HttpMethod;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.MockMvcBuilder;
import org.springframework.test.web.servlet.RequestBuilder;
import org.springframework.test.web.servlet.request.MockHttpServletRequestBuilder;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;
import org.springframework.test.web.servlet.setup.MockMvcBuilders;

import static org.springframework.test.web.servlet.setup.MockMvcBuilders.*;


import static org.junit.Assert.*;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;

/**
 * create by songweiliang on 2018/8/27
 */
public class MainConTrollerTest {
//
//    @Test
//     public void  test() throws Exception {
//         MainConTroller mainConTroller = new MainConTroller();
//         assertEquals("test",mainConTroller.firstRest());
//
//        MockMvc mockMvc = MockMvcBuilders.standaloneSetup(mainConTroller).build();
//        mockMvc.perform(get("/main/test")).andExpect(view().name("test"));
//
//     }
}